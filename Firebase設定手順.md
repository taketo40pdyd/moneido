# Moneido を Firebase バックエンドに接続する手順

このアプリはコードの変更が完了しています。あとは **あなた自身の Firebase プロジェクト** を作成し、
2箇所に設定値を貼り付ければ動きます。

---

## 1. Firebaseプロジェクトを作成する

1. https://console.firebase.google.com/ を開き、Googleアカウントでログイン
2. 「プロジェクトを追加」→ プロジェクト名を入力(例: `moneido`)→ 作成
3. Google Analyticsは不要であれば無効のままでOK

## 2. Authentication(ログイン機能)を有効化する

1. 左メニュー「構築」→「Authentication」→「始める」
2. 「Sign-in method」タブで以下の3つを有効化:
   - **メール/パスワード**: 有効にするだけでOK
   - **Google**: 有効化 → プロジェクトのサポートメール等を選んで保存
   - **匿名**: 有効にするだけでOK(アプリの「ゲストとして続ける」機能に必要)
3. Google を有効化すると、その設定画面内に **「ウェブ SDK の構成」** という欄が表示され、
   「ウェブクライアント ID」が確認できますが、この構成では**貼り付ける必要はありません**
   (Firebaseの `signInWithPopup` を使うため、コード側の追加設定は不要です)。

## 3. Firestore Database を有効化する

1. 左メニュー「構築」→「Firestore Database」→「データベースの作成」
2. ロケーション(例: `asia-northeast1` = 東京)を選んで作成
3. 「本番環境モード」で作成してOK(セキュリティルールは手順5で設定します)

## 4. ウェブアプリを登録し、設定値(firebaseConfig)を取得する

1. プロジェクトの概要ページ左上の「⚙(プロジェクトの設定)」→「全般」タブ
2. 下の方の「マイアプリ」→ `</>`(ウェブ)アイコンをクリック
3. アプリのニックネームを入力(例: `moneido-web`)→ 登録
4. 表示される `firebaseConfig = { apiKey: "...", ... }` をコピー
5. `index.html` を開き、`firebaseConfig` の中身(`YOUR_API_KEY` などの部分)を、
   コピーした値にそのまま置き換えてください。

```js
const firebaseConfig = {
  apiKey: "コピーした値",
  authDomain: "コピーした値",
  projectId: "コピーした値",
  storageBucket: "コピーした値",
  messagingSenderId: "コピーした値",
  appId: "コピーした値",
};
```

## 5. Firestore セキュリティルールを設定する

1. Firebaseコンソール →「Firestore Database」→「ルール」タブ
2. 添付した `firestore.rules` の内容を全て貼り付けて「公開」

このルールは、
- 個人データ(口座・取引履歴・設定など)は **本人だけ** が読み書きできる
- 家族共有データは、**招待コードを知っている人(=サインイン済みの人)** が読み書きできる

という、元のアプリの共有の考え方をそのまま踏襲しています。

## 6. 承認済みドメインを確認する(公開する場合)

Authentication →「Settings」→「承認済みドメイン」に、実際にこのアプリを配信するURL
(例: `your-name.github.io`)を追加してください。`localhost` は最初から登録済みです。

## 7. 動作確認

`index.html` をウェブサーバー経由で開いて(`file://` では動きません)、
- 「ゲストとして続ける」→ 中身が使えるか
- 「メールアドレスでログイン」→ 新規登録できるか
- 「Googleでログイン」→ Googleボタンが表示され、ログインできるか
- Firebaseコンソールの「Authentication」「Firestore Database」にユーザーとデータが
  実際に作成されているか

を確認してください。

---

## 変更点まとめ(何が変わったか)

- **保存先**: これまでの「ブラウザのlocalStorage」から「Firestore(クラウド)」に変更されました。
  同じアカウントでログインすれば、別の端末からでも同じデータにアクセスできます。
- **ログイン**: 自作の簡易ログインから、本物の **Firebase Authentication** に置き換えました。
  - パスワードは平文やハッシュとしてアプリ側に保存されなくなり、Firebaseが安全に管理します。
  - 「パスワードを忘れた場合」は、実際にリセット用メールが届くようになりました。
  - 「ゲストとして続ける」は匿名認証(Anonymous Auth)になりました。ゲストのまま
    後からメール/Googleアカウントに「アップグレード」すると、それまでのデータは
    **そのまま引き継がれます**(同じユーザーIDに紐付けられるため)。

## 既知の制限

- Firestoreの1ドキュメントは最大1MB程度までです。取引件数が非常に多くなると、
  現在のようにデータをまとめて1つのドキュメントに保存する方式ではいずれ上限に近づく
  可能性があります。将来的に取引を1件ずつ別ドキュメントに分ける設計への見直しが
  必要になるかもしれません(現状の利用規模であれば当面問題ありません)。
- Firebaseの無料プラン(Sparkプラン)には読み書き回数などの上限があります。個人・家族利用
  であれば通常は無料枠内に収まります。
